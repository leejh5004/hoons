rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🚨 임시 테스트용 - 모든 접근 허용 🚨
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 